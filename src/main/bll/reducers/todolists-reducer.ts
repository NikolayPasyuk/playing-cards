export type InitialStateType = typeof initialState

const initialState = {}

export const todolistsReducer = (state: InitialStateType = initialState, action: any): InitialStateType => {
    return state
}
