export type InitialStateType = typeof initialState

const initialState = {}

export const tasksReducer = (state: InitialStateType = initialState, action: any): InitialStateType => {
    return state
}
